import { ShopVistaValidators } from './shop-vista-validators';

describe('ShopVistaValidators', () => {
  it('should create an instance', () => {
    expect(new ShopVistaValidators()).toBeTruthy();
  });
});
